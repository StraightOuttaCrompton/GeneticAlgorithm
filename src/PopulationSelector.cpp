#include "PopulationSelector.h"

template<class G, class F>
IMatingPool<G, F>
PopulationSelector<G, F>::getMatingPool(IPopulation<G> population, IFitnessFunction<G, F> fitnessFunction) {
    // iterate over population
    // for each item in population
    // matingpool.add(item, fitness)
}